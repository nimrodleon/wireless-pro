export interface Device {
  _id: string;
  ipAddress: string;
  mode: string;
  name: string;
  userName: string;
  password: string;
  ssid: string;
  coverage: string;
  tower: string;
  tramo: string;
  accessPoint: string;
}
